
mypdflat()
{
pdflatex $1 ;pdflatex $1
}
alias lat=mypdflat

# ------comandos
ged() {
	gedit $1 &
}
alias g=ged

alias pu='rm *~ *.log *.aux *.dvi *# *.toc *.out'

alias shutdown='sudo shutdown –h now'

alias matrix='xterm -fullscreen -e cmatrix'

vimear() {
	gnome-terminal --window-with-profile=Vim -e "vim $1"
}

alias vime=vimear	#este necesita un nuevo profile de gnome-terminal que se llame Vim

alias moc='gnome-terminal --geometry 110x25+400+300 -e mocp'

# alias moc='sudo  mount /dev/sda3 /media/1E784E3B784E1247;gnome-terminal --geometry 110x25+400+300 -e mocp'  este monta la unidad antes, hace falta crear la carpeta /media/1E784E3B784E1247


pdfear() {
	okular $1 &
}
alias pdf=pdfear

# -----navegacion

alias docs='cd ~/Documentos'

alias tp='cd Documentos/TPs/Algo\ 2/'


alias prog='cd ~/Documentos/Programacion'

alias ..='cd ..'

alias ...='cd ../..'

alias ....='cd ../../..'

alias mus='sh ~/bin/mus.sh'

alias milagro='ssh nroulet@milagro.dc.uba.ar'

alias git?='echo Para pullear y repushear: ; echo "    git pull origin master"; echo "    git commit -a -m <nombreDeCommit>"; echo "    git push origin master"; echo "Para agregar archivos: git add <nombreArchivo>"; echo "Para configurar la carpeta con git:"; echo "    git init"; echo "    git remote add origin <direccionDelRepo>"; echo "    git pull origin master"; echo "    git fetch origin"; echo "   git pull origin master"; echo "Para ver estado: git status"; echo "Para configurar mail y nombre de usuario:"; echo "    git config --global user.name nombreEntreComillas"; echo "    git config --global user.email mailSinComillas"'

function setgov ()
{
    echo "$1" | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor 
}

alias governors='cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors '

alias milagro='ssh nroulet@milagro.dc.uba.ar'
